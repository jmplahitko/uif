enum RequestCache {
	default = 'default',
	noStore = 'no-store',
	reload = 'reload',
	noCache = 'no-cache',
	forceCache = 'force-cache',
	onlyIfCached = 'only-if-cached'
}

export default RequestCache;